#pragma once
#ifndef mytype
#define mytype
enum BASIC { INT, FLOAT, STR, BOOL };
enum COMP { EQ, NEQ, GEQ, LEQ, LS, GT };
enum FLOWSTMT { BREAK, CONTINUE, RETURN };
enum AUGASSIGN { EQUAL, ADD, MINUS, DIV, INTDIV, MOL, UNDEF };

#endif